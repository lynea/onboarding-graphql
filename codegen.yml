overwrite: true
schema: "./src/graphql/schema.graphql"
documents: null
generates:
  src/graphql/types/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
